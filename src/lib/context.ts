/**
 * This interface will be used to pass additional information
 * about the element whose name is being computed.
 */
export interface Context {
  ariaLabelledbyReference?: boolean;
  nativeTextAlternativeReference?: boolean;
}
